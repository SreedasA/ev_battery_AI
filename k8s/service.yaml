apiVersion: v1
kind: Service
metadata:
  name: ev-battery-ai
spec:
  type: NodePort
  selector:
    app: ev-battery-ai
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
